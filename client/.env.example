# VITE_API_URL should be the base server URL WITHOUT a trailing '/api' to avoid double prefixes.
# The client code calls endpoints like '/api/admin/...', so the base should be just the server origin.
VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000
